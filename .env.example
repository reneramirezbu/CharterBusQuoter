# Server Configuration
PORT=5000

# Google Maps API (required for location autocomplete)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# CORS Configuration
# Comma-separated list of domains that can access the API
# For WordPress integration, add your WordPress domain
ALLOWED_ORIGINS=https://your-wordpress-site.com,http://localhost:3000

# Frontend URL for redirects (optional)
FRONTEND_URL=http://localhost:3000